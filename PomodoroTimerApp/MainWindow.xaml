<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PomodoroTimerApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PomodoroTimerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PomodoroTimerApp">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock x:Name="inactivityTimerTextBlock" FontSize="24" Margin="10" HorizontalAlignment="Center" Foreground="Green" Visibility="Collapsed"/>
        <TextBlock x:Name="timerTextBlock" FontSize="48" Margin="10" HorizontalAlignment="Center"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="myButton" Margin="10" Click="myButton_Click">Start Timer</Button>
            <Button x:Name="stopButton" Content="Stop Timer" Click="stopButton_Click" IsEnabled="False"/>
        </StackPanel>
    </StackPanel>
</Window>
